{
  "name": "d3f-face",
  "version": "1.0.0",
  "private": true,
  "description": "Página estática de Eduardo Claas — Dieta das 3 Fases",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "node --check script.js && node --check player.js && npm test"
  }
}
